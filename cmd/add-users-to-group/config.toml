bearer_token = "<BEARER_TOKEN>"
admin_user_id = "<ADMIN_USER_ID>"
admin_portal_address = "10.254.148.112:9443"
worker_count = 16
dry_run = false
x
# Required: the group that the users below will be added to.
group_id = "<GROUP_ID>"

# Required: list of user emails to add to the group. Each email is resolved to a
# user ID, then the users are added in batches of 10.
emails = []

