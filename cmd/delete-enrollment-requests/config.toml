bearer_token = "<BEARER_TOKEN>"
admin_user_id = "<ADMIN_USER_ID>"
admin_portal_address = "<ADMIN_PORTAL_ADDRESS>"
worker_count = 16
organizational_unit_id = "<ORGANIZATIONAL_UNIT_ID>"
dry_run = false

# Optional: list of user emails whose enrollment requests should NOT be deleted
exclude_emails = []
