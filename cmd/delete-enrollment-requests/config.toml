bearer_token = "CHANGE_ME"
admin_user_id = "CHANGE_ME"
admin_portal_address = "10.0.0.1:9443"
worker_count = 16
organizational_unit_id = "CHANGE_ME"
dry_run = true

# Optional: list of user emails whose enrollment requests should NOT be deleted
exclude_emails = []
